# KickoutNonASCIINames

一个 Terraria TShock 服务器插件，用于自动踢出昵称中含有非 ASCII 字符的玩家。

## 使用说明

### 默认行为

插件默认启用自动踢出功能，当含有非 ASCII 字符昵称的玩家加入服务器时会被自动踢出。

#### 管理命令

使用以下命令需要相应的权限：`KickoutNonASCIINames`

#### 基础命令
`/kickoutnonasciiname` `/koin`
#### 子命令
`run` -- 手动执行一次踢出

`enable` -- 启用自动踢出

`disable` -- 禁用自动踢出
